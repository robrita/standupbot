# FastAPI
CORS_ORIGINS=http://localhost:5173

# Azure Authentication (for local development)
# Option 1: Run `az login` in terminal (no env vars needed)
# Option 2: Use Service Principal (set the three variables below)
# AZURE_TENANT_ID=your-tenant-id
# AZURE_CLIENT_ID=your-service-principal-app-id
# AZURE_CLIENT_SECRET=your-service-principal-secret

# Azure Voice Live API with AI Foundry Agent
# Required environment variables:
# Voice Live resource endpoint (Foundry resource or older Cognitive Services endpoint)
AZURE_VOICE_LIVE_ENDPOINT=https://YOUR-RESOURCE-NAME.services.ai.azure.com
# For older resources:
# AZURE_VOICE_LIVE_ENDPOINT=https://YOUR-RESOURCE-NAME.cognitiveservices.azure.com
AI_FOUNDRY_PROJECT_NAME=YOUR_PROJECT_NAME
AI_FOUNDRY_AGENT_ID=YOUR_AGENT_ID

# Optional: API version (default: 2025-10-01)
AZURE_VOICE_LIVE_API_VERSION=2025-05-01-preview

# Optional: Text-to-Speech (TTS) voice configuration
AZURE_VOICELIVE_TTS_VOICE=en-US-GuyNeural
AZURE_VOICELIVE_TTS_VOICE_TYPE=azure-standard
# AZURE_VOICELIVE_TTS_TEMPERATURE=0.8

# Optional: Voice Activity Detection (VAD) configuration
# AZURE_VOICELIVE_VAD_THRESHOLD=0.3
# AZURE_VOICELIVE_VAD_PREFIX_PADDING_MS=200
# AZURE_VOICELIVE_VAD_SILENCE_DURATION_MS=200

# Optional: Audio configuration
# AZURE_VOICELIVE_AUDIO_SAMPLING_RATE=24000

# Optional: Transcription languages (comma-separated language codes)
AZURE_VOICE_LIVE_TRANSCRIPTION_LANGUAGES=en-US

